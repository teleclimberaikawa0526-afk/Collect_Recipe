# EAS Build（クラウドビルド）の手順書

Expo アプリを Android スマホにインストールするための「EAS Build」の手順まとめです。
PCに重たい開発環境（Android Studio等）を入れなくても、クラウド上でアプリファイル（APK）を作成できます。

---

## 1. 初回のみ必要な準備（ログイン）
EAS（Expo Application Services）を利用するには、Expoアカウントでのログインが必要です。

1. ターミナルでアプリのフォルダ（`Collect_Recipe`）を開きます。
2. 以下のコマンドを実行します。
   ```bash
   npx eas-cli login
   ```
   ※途中で `Need to install the following packages: eas-cli. Ok to proceed? (y)` と聞かれたら、半角の `y` を入力して `Enter` を押してください。
3. メールアドレスとパスワードを入力してログインを完了させます。（アカウントがない場合は https://expo.dev/ で Sign Up してください）

---

## 2. 開発用アプリのビルド（コード修正をすぐに反映したい場合）
開発中、コードの修正内容をPCからスマホへリアルタイムに反映させるための「開発用アプリ」を作成します。

1. ターミナルで以下のコマンドを実行します。
   ```bash
   npx eas-cli build -p android --profile development
   ```
   ※「eas.jsonを作成しますか？」「Application IDを設定しますか？」等の質問が出たら、すべて `Enter` キー（Yes）で進めてOKです。
2. 10〜15分ほど待つと、ターミナルに **QRコード** が表示されます。
3. スマホのカメラでQRコードを読み取り、アプリ（`.apk`）をダウンロード＆インストールします。
4. インストール後、PCのターミナルで以下のコマンドを実行します。
   ```bash
   npx expo start
   ```
5. 表示された大きなQRコードを、**スマホに入れた開発用アプリのカメラ機能**で読み取ります。
   👉 これ以降は、PCでコードを上書き保存（Ctrl+S）するだけで、一瞬でスマホ画面が更新されます！

---

## 3. 完成版アプリのビルド（家族にシェアしたい場合）
開発が終わり、PCと繋がなくても単独で動く「完成版」を作成して家族に配布したい場合の手順です。

1. ターミナルで以下のコマンドを実行します。
   ```bash
   npx eas-cli build -p android --profile preview
   ```
2. こちらも10〜15分ほどで完成し、ターミナルに **インストール用のURLとQRコード** が表示されます。
3. このURLやQRコードの画像を、LINEやメールでご家族に送ってください。
4. スマホからURLを開くかQRコードを読み取ると、アプリのダウンロードとインストールができます。

**【⚠️注意点：提供元不明のアプリについて】**
Google Playストアを通していないため、インストール時に「有害なファイルの可能性があります」「提供元不明のアプリです」といった警告が出ることがあります。ご自身で作成した安全なアプリですので、「詳細設定」などからインストールを許可（無視してインストール）して進めてください。
